---

- hosts: "{{ host }}"

  vars:
    lsd_path: "/usr/local/bin"
    starship_path: "/usr/local/bin"

  roles:

    - role: bootstrap-common
      tags:
        - common

    - role: lsd
      tags:
        - lsd

    - role: starship
      tags:
        - starship
